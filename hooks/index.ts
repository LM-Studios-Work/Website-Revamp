/**
 * Hooks Index
 * 
 * Centralized exports for all custom hooks.
 * 
 * Usage:
 *   import { useContactForm, useMobile, useToast } from "@/hooks";
 */

export { useContactForm, type ContactFormData } from "./use-contact-form";
export { useIsMobile } from "./use-mobile";
export { useToast, toast } from "./use-toast";
